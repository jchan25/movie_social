<div class="page-header">
  <h1>Add Genre</h1>
</div>

<% if @genre.errors.any? %>
  <% @genre.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>


<form action="/create_genre" class="form-horizontal">
  <!-- Label and input for genre_name -->
  <div class="form-group">
    <label for="genre_name" class="col-sm-2 control-label">
      Genre name
    </label>
    <div class="col-sm-10">
      <input type="text" id="genre_name" name="genre_name" placeholder="Genre name" class="form-control" value="<%= @genre.genre_name %>">
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <input type="submit" value="Create Genre" class="btn btn-primary">
    </div>
  </div>
</form>
