
<div class="page-header">
  <h1>Select Favorite Genres</h1>
</div>

<body>Choose the genres of movies you would like to show up in your queue. </body>

<p>
</br>

<% if @user_favorited_genre.errors.any? %>
  <% @user_favorited_genre.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>


<form action="/create_user_favorited_genre" class="form-horizontal">



<% if current_user.present? %>


  <% else %>

<% end %>




<% Genre.all.each do |genre| %>
  <label>
    <input type="checkbox" name="genre_<%= genre.genre_name %>" value="<%= genre.id %>">
    <%= genre.genre_name %>
  </label>
  <!-- Parameters user_ID 11 genre_Drama 1 genre_ID 9 -->
<% end %>


  <!-- Label and input for genre_ID -->
 <!--  <div class="form-group">
    <label for="Genre" class="col-sm-2 control-label">
      Genre
    </label>
    <div class="col-sm-10">
     <%= select_tag(:genre_ID, options_from_collection_for_select(Genre.all, :id, :genre_name, @user_favorited_genre.genre_ID), :class => "form-control") %>
    </div>
  </div> -->

  <div class="form-group">
    <div class="col-sm-offset-0 col-sm-10">
      <input type="submit" value="Update Genre Preferences" class="btn btn-primary">
    </div>
  </div>
</form>


